/* Estilos generales */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  background-color: #f4f4f4;
}

/* Encabezado */
header {
  background-color: #006400; /* Verde fútbol */
  color: white;
  text-align: center;
  padding: 20px;
}

header .logo {
  max-width: 120px;
  display: block;
  margin: 0 auto 10px;
}

/* Secciones */
section {
  padding: 20px;
  max-width: 1000px;
  margin: auto;
}

/* Cards de servicios */
.cards {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}

.card {
  background: white;
  padding: 15px;
  border-radius: 10px;
  flex: 1;
  min-width: 250px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

/* Galería */
.imagenes {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.imagenes img {
  width: 100%;
  max-width: 300px;
  border-radius: 8px;
}

/* Contacto */
form {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

input, textarea, button {
  padding: 10px;
  font-size: 1em;
}

button {
  background: #006400;
  color: white;
  border: none;
  cursor: pointer;
}

button:hover {
  background: #004d00;
}

/* Pie de página */
footer {
  background: #111;
  color: white;
  text-align: center;
  padding: 10px;
}

/* Responsive */
@media (max-width: 600px) {
  .cards {
    flex-direction: column;
  }
  .imagenes {
    flex-direction: column;
    align-items: center;
  }
}
